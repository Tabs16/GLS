-
main context start
    function start : SayHello void name string
        print : { concatenate : ("Hello, ") name "!" }
    function end

    main start
        function : SayHello "GLS"
    main end
main context end
-
